% A = [1 2 3; 4 5 6];
% [S,U,V] = dm_svd(A);

img = imread('../Images/classic/mandril_gray.tif');
img = im2double(img);
[S,U,V] = dm_svd(img);
img_reconstructed = U*S*V';
S_sum = sum(diag(S));
threshold = 0.5;

